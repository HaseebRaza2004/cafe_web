import DealForm from "@/components/custom_components/admin/DealForm";

export const metadata = {
  title: "Add Deal | Admin Panel",
};

export default function AddDealPage() {
  return <DealForm />;
}